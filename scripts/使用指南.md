# Vibe Standards 自动化工具使用指南

> 让所有项目自动遵循 vibe-standards 规范

---

## 📦 安装 (一次性操作)

### 1. 安装 Git Hook

在项目根目录执行：

```bash
# 复制 pre-commit hook
cp vibe-standards/scripts/pre-commit-hook.sh .git/hooks/pre-commit
chmod +x .git/hooks/pre-commit
```

### 2. 验证安装

```bash
# 测试 linter
node vibe-standards/scripts/vibe-lint.js
```

---

## 🚀 自动化流程

### 本地开发

当你尝试提交代码时：

```bash
git add .
git commit -m "feat: 添加新功能"
```

Git Hook 会**自动运行**：
1. 检查 `docs/` 下所有 `.md` 文件是否使用中文命名
2. 检查代码目录（`src/`, `components/` 等）是否使用 kebab-case
3. 如果有违规，**阻止提交**并显示错误

### 示例输出

```
🔍 Vibe Standards 规范检查...

📝 检查文档文件命名...
❌ 发现 2 个文档文件命名违规：
  ./docs/01-规范体系/ui-design-spec.md
  建议: UI设计规范.md

❌ 提交被阻止：请先修复规范违规问题
```

---

## 🛠️ 手动检查

随时运行 linter：

```bash
node vibe-standards/scripts/vibe-lint.js
```

---

## 📋 规范清单

| 检查项     | 规则       | 错误示例            | 正确示例          |
| ---------- | ---------- | ------------------- | ----------------- |
| 文档文件名 | 使用中文   | `ui-design-spec.md` | ✅ `UI设计规范.md` |
| 代码目录名 | kebab-case | `UserProfile/`      | ✅ `user-profile/` |

---

## 🔧 CI/CD 集成 (可选)

### GitHub Actions

创建 `.github/workflows/vibe-lint.yml`：

```yaml
name: Vibe Standards Check

on: [push, pull_request]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: true
      - uses: actions/setup-node@v3
      - run: node vibe-standards/scripts/vibe-lint.js
```

---

**维护者**：Vibe Standards Team  
**版本**：V1.0
