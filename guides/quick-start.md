# Vibe Standards 快速开始指南

本指南帮助你在 5 分钟内快速上手 Vibe Standards 规范体系。

---

## 📦 安装 Claude Code

```bash
# 安装 Claude Code CLI
npm install -g @anthropics/claude-code

# 验证安装
claude --version
```

---

## 🚀 使用 Vibe Standards

### 方式一：作为插件使用（推荐）

1. **Clone 仓库到本地**
```bash
git clone https://github.com/PANGKAIFENG/-vibe-standards-.git ~/vibe-standards
```

2. **配置 Claude Code**

编辑 `~/.claude/settings.json`，添加插件路径：
```json
{
  "pluginPaths": [
    "~/vibe-standards"
  ]
}
```

3. **重启 Claude Code**
```bash
# 重启后，Vibe Standards 的所有规范会自动加载
```

### 方式二：直接复制到全局配置

```bash
# 复制所有规范到 Claude 全局目录
cd ~/vibe-standards
cp -r agents ~/.claude/agents/
cp -r commands ~/.claude/commands/
cp -r skills ~/.claude/skills/
cp -r rules ~/.claude/rules/
cp -r contexts ~/.claude/contexts/
```

---

## 💡 快速使用示例

### 1. 使用 PRD 规范编写产品需求

在你的项目中，AI 会自动遵循 PRD 规范：

```bash
# 在 Claude Code 中
用户：帮我写一个"用户登录"功能的 PRD

AI：我会按照 PRD 规范的 8 大章节来编写：
1. 功能目标
2. 用户场景
3. 入口位置
4. 界面布局与交互
5. 操作流程
6. 异常处理
7. 验收标准
8. 产品记忆库
```

### 2. 使用命令快速执行任务

```bash
# 创建实现计划
/plan 实现用户登录功能

# 测试驱动开发
/tdd 编写用户登录的测试用例

# 代码审查
/code-review 审查登录功能的代码

# E2E 测试
/e2e 生成登录功能的 E2E 测试
```

### 3. 使用代理处理复杂任务

AI 会自动调用专业代理：

```bash
# 规划复杂功能
用户：帮我规划一个完整的用户管理系统
AI：我会使用 planner 代理来创建详细的实现计划

# 架构设计决策
用户：用户管理系统应该用什么架构？
AI：我会使用 architect 代理来评估不同架构方案

# 安全审查
用户：审查用户管理系统的安全性
AI：我会使用 security-reviewer 代理进行全面的安全分析
```

---

## 📚 核心规范说明

### Rules（全局规则）

AI 会自动遵循这些规则：

1. **PRD 生成规范** (`01-requirement-prd-standard.md`)
   - 8 大章节标准模板
   - 用户场景 + 界面布局 + 操作流程
   - 异常处理 + 验收标准

2. **代码风格** (`coding-style.md`)
   - 不可变性原则
   - 文件组织（200-800 行）
   - 错误处理和输入验证

3. **测试要求** (`testing.md`)
   - TDD 工作流（先写测试）
   - 80% 测试覆盖率
   - 单元测试 + 集成测试 + E2E 测试

4. **Git 工作流** (`git-workflow.md`)
   - 约定式提交（feat/fix/docs...）
   - PR 工作流
   - 功能实现流程

5. **安全规范** (`security.md`)
   - 禁止硬编码 secrets
   - 输入验证
   - SQL 注入 / XSS / CSRF 防护

6. **代理编排** (`agents.md`)
   - 何时使用代理
   - 并行任务执行
   - 多视角分析

7. **性能优化** (`performance.md`)
   - 模型选择策略（Haiku/Sonnet/Opus）
   - 上下文窗口管理
   - Ultrathink + Plan Mode

8. **通用模式** (`patterns.md`)
   - API 响应格式
   - 自定义 Hooks
   - Repository 模式

9. **Hooks 系统** (`hooks.md`)
   - PreToolUse / PostToolUse / Stop
   - 自动化检查和格式化

### Agents（专业助手）

10 个专业代理，按需自动调用：

| 代理 | 用途 | 触发场景 |
|------|------|---------|
| **planner** | 功能规划 | 复杂功能、重构 |
| **architect** | 系统设计 | 架构决策、技术选型 |
| **tdd-guide** | TDD 指导 | 新功能、bug 修复 |
| **code-reviewer** | 代码审查 | 代码编写后自动触发 |
| **security-reviewer** | 安全审查 | 提交前、处理敏感数据 |
| **build-error-resolver** | 构建修复 | 构建失败时 |
| **e2e-runner** | E2E 测试 | 关键用户流程 |
| **refactor-cleaner** | 代码清理 | 死代码、冗余代码 |
| **doc-updater** | 文档更新 | 功能变更后 |
| **database-reviewer** | 数据库审查 | SQL 查询、schema 设计 |

### Skills（工作流知识库）

14 个技能包，提供领域知识：

- **coding-standards** - TypeScript/JavaScript/React 最佳实践
- **frontend-patterns** - React/Next.js 开发模式
- **backend-patterns** - API 设计、数据库优化
- **tdd-workflow** - 测试驱动开发方法论
- **security-review** - 安全检查清单
- **postgres-patterns** - PostgreSQL 查询优化
- **continuous-learning** - 自动提取会话模式
- **iterative-retrieval** - 渐进式上下文细化

---

## 🎯 适用场景

### 场景 1：产品经理编写 PRD

```bash
# 1. 描述功能需求
用户：帮我写一个"商品搜索"功能的 PRD

# 2. AI 按 PRD 规范生成
AI：我会按照以下结构生成 PRD：
- 功能目标：帮助用户快速找到想要的商品
- 用户场景：小王想买一件羽绒服...
- 入口位置：首页顶部搜索框
- 界面布局：搜索框 + 筛选面板 + 搜索结果列表
- 操作流程：输入关键词 → 点击搜索 → 显示结果
- 异常处理：无结果、网络错误、输入为空
- 验收标准：搜索响应 < 2s、支持模糊匹配
```

### 场景 2：开发人员实现功能

```bash
# 1. 规划实现方案
/plan 实现商品搜索功能

# 2. TDD 开发
/tdd 编写搜索功能的测试用例

# 3. 实现代码
用户：实现搜索功能
AI：[遵循 coding-style 规则，生成不可变、模块化的代码]

# 4. 代码审查
/code-review
AI：[使用 code-reviewer 代理检查代码质量、安全性]

# 5. 安全审查
AI：[使用 security-reviewer 代理检查 SQL 注入、XSS]

# 6. E2E 测试
/e2e 生成搜索功能的 E2E 测试
```

### 场景 3：团队协作

```bash
# 产品经理
用户（PM）：帮我写一个"用户反馈"功能的 PRD
AI：[按 PRD 规范生成完整的产品需求文档]

# 开发人员
用户（Dev）：基于 PRD 实现用户反馈功能
AI：[遵循 coding-style、testing、security 规则实现]

# 测试人员
用户（QA）：生成用户反馈功能的测试用例
AI：[使用 tdd-workflow 技能生成完整测试用例]
```

---

## 🔧 自定义配置

### 覆盖规则

如果你的项目有特殊规范，可以在项目根目录创建 `.claude/` 目录，覆盖全局规则：

```bash
# 项目特定的规则
mkdir -p .claude/rules/
echo "# 项目特定的代码风格" > .claude/rules/coding-style.md
```

### 添加自定义技能

```bash
# 创建项目特定的技能
mkdir -p .claude/skills/my-project-patterns/
echo "# SKILL: 我的项目模式" > .claude/skills/my-project-patterns/SKILL.md
```

---

## 📖 进一步学习

- **ECC 简明指南**：[Everything Claude Code Shorthand Guide](https://x.com/affaanmustafa/status/2012378465664745795)
- **ECC 详细指南**：[Everything Claude Code Longform Guide](https://x.com/affaanmustafa/status/2014040193557471352)
- **PRD 规范详解**：[rules/01-requirement-prd-standard.md](../rules/01-requirement-prd-standard.md)
- **Claude Code 官方文档**：https://claude.ai/claude-code

---

## ❓ 常见问题

### Q1: 规范会自动生效吗？

是的。一旦你配置了 Vibe Standards，AI 会自动遵循所有规范：
- Rules 会自动应用到每个任务
- Agents 会在需要时自动调用
- Commands 可以通过 `/` 命令快速执行

### Q2: 如何知道 AI 正在遵循规范？

AI 的输出会明确体现规范要求：
- 生成的 PRD 会包含 8 大章节
- 代码会遵循不可变性原则
- 测试会达到 80% 覆盖率
- Commit message 会遵循约定式提交格式

### Q3: 规范太严格怎么办？

你可以：
1. 在项目级别覆盖规则
2. 临时禁用某些规则（编辑 `~/.claude/rules/`）
3. 根据团队需求调整规范内容

### Q4: 如何更新规范？

```bash
# 拉取最新版本
cd ~/vibe-standards
git pull

# 如果使用插件模式，重启 Claude Code 即可
# 如果使用复制模式，需要重新复制文件
```

---

**开始使用 Vibe Standards，让 AI 按规范为你工作！** 🚀
